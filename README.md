Quick Synchronize Deployment
==================

A idea plugin tool that provide a quick way to set up deployment.

- The plugin use mount type to synchronize file.
- Please config the target root path in the settings.

